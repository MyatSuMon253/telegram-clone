'use client'

import TelegramLoading from "./TelegramLoading"

const Loading = () => {
  return (
      <div className="min-h-screen w-full flex justify-center items-center"><TelegramLoading /></div>
  )
}

export default Loading